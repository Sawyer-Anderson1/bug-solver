# find_files Tool responses

## found_matches

Ran a grep for pattern `{text_pattern}` through the filesystem from the root of the repository, it succeeded with matches:
`{raw_data}`.

Files searched through:
{files}

Matched files:
{matched_files}

Unreadable files, that could not be checked:
{unreadable_files}

## no_matches

Ran a grep for pattern `{text_pattern}` through the filesystem from the root of the repository, BUT no matches were found.

Files searched through:
{files}

Unreadable files, that could not be checked:
{unreadable_files}

Error message:
`{error_details}`

_Action Required: You may have to choose another pattern to search for._
