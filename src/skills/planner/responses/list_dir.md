# list_dir Tool responses

## searched_recursively_success

Ran a recursive search from the directory `{dir}` down to its subdirectories.
`{raw_data}`

Visual representation of the repository:
`{visual_repo_structure}`

Path's of the repository structure:
`{path_repo_structure}`

Files:
{files}

Directories:
{dirs}

## searched_shallow_success

Ran a shallow search within the direcotry `{dir}`.
`{raw_data}`

File:
{files}

Subdirectories:
{dirs}

## dir_non_existent

The directory `{dir}` was not found to be existent.

## not_dir

The path given for `{dir}` was found not to be a directory.
