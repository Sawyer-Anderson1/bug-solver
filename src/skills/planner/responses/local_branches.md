# local_branches Tool responses

## listed_branches

The tool call to get a list of all the local branches of a developers repository ran successfully.

Local branches:
{local_branches}

## subprocess_error

Ran into a CalledProcessError when attempting to run a git branch command to retrieve all local branches in repository.

The error details:
raw_data: `{raw_data}`
error_details: `{error_details}`
